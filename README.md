# ROBLOX | Multi-Instance Memory Leak Patch
- Required: Python 3.10+
The current Roblox web client tends to suffer memory leaks when multiple instances are open at a time.
This simple script fixes this by detecting ghost processes and killing them. 
